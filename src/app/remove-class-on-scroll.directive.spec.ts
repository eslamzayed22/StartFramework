import { RemoveClassOnScrollDirective } from './remove-class-on-scroll.directive';

describe('RemoveClassOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveClassOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
